# Frontend Environment Variables

# The base URL for the backend API server.
# In development, it points to the Express dev server port (e.g. 5050).
# In production, it should point to your deployed backend domain.
VITE_API_URL=http://localhost:5050/api
